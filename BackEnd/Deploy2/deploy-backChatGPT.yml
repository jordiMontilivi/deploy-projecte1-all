name: Deploy Backend (DEV & PROD)

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop]
  workflow_dispatch:

# Evita deploys simultanis
concurrency:
  group: deploy-backend-${{ github.ref }}
  cancel-in-progress: true

env:
  TARGET_ENV: ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
  TARGET_ROLE: ${{ github.ref == 'refs/heads/main' && 'backend' || 'all' }}
  TARGET_HOST: ${{ github.ref == 'refs/heads/main' && vars.EC2_HOST_PROD || vars.EC2_HOST }}
  TARGET_KEY: ${{ github.ref == 'refs/heads/main' && secrets.EC2_SSH_KEY_PROD || secrets.EC2_SSH_KEY }}
  TARGET_USER: ${{ vars.EC2_USER }}

jobs:
  deploy-backend:
    runs-on: ubuntu-latest

    steps:
      - name: 1. Checkout repositori
        uses: actions/checkout@v4

      # PHP SETUP
      - name: 2. Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
          tools: composer

      # COMPOSER CACHE
      - name: 3. Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: ~/.composer/cache
          key: composer-${{ runner.os }}-${{ hashFiles('**/composer.lock') }}
          restore-keys: composer-${{ runner.os }}-

      # BUILD (PROD ONLY)
      - name: 4. Build vendor (PROD)
        if: github.ref == 'refs/heads/main'
        run: |
          composer install \
            --no-dev \
            --optimize-autoloader \
            --no-progress \
            --no-interaction

      # SSH CONFIG
      - name: 5. Configure SSH
        run: |
          mkdir -p ~/.ssh

          echo "${{ env.TARGET_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

          ssh-keyscan -H ${{ env.TARGET_HOST }} >> ~/.ssh/known_hosts

      # SERVER PROVISIONING
      - name: 6. Provision server (idempotent)
        run: |
          scp ./full-setup.sh \
            ${{ env.TARGET_USER }}@${{ env.TARGET_HOST }}:/home/${{ env.TARGET_USER }}/full-setup.sh

          ssh ${{ env.TARGET_USER }}@${{ env.TARGET_HOST }} << 'EOF'
            chmod +x full-setup.sh
            sudo bash full-setup.sh $TARGET_ENV $TARGET_ROLE
            rm -f full-setup.sh
          EOF

      # RSYNC DEV
      - name: 7A. Sync files (DEV)
        if: github.ref == 'refs/heads/develop'
        run: |
          rsync -az --delete \
            --exclude '.git/' \
            --exclude '.github/' \
            --exclude 'var/' \
            --exclude 'vendor/' \
            --exclude '.env.local' \
            ./ ${{ env.TARGET_USER }}@${{ env.TARGET_HOST }}:/var/www/backend/

      # RSYNC PROD
      - name: 7B. Sync files (PROD)
        if: github.ref == 'refs/heads/main'
        run: |
          rsync -az --delete \
            --exclude '.git/' \
            --exclude '.github/' \
            --exclude 'var/' \
            --exclude 'tests/' \
            ./ ${{ env.TARGET_USER }}@${{ env.TARGET_HOST }}:/var/www/backend/

      # SYMFONY POST DEPLOY
      - name: 8. Symfony post deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.TARGET_HOST }}
          username: ${{ env.TARGET_USER }}
          key: ${{ env.TARGET_KEY }}
          script: |

            echo " Deploy Symfony (${{ env.TARGET_ENV }})"

            cd /var/www/backend

            if [ "${{ env.TARGET_ENV }}" = "dev" ]; then

                echo " DEV setup"

                export COMPOSER_ALLOW_SUPERUSER=1
                composer install --optimize-autoloader

                php bin/console cache:clear --env=dev

                php bin/console doctrine:migrations:migrate \
                    --allow-no-migration \
                    --no-interaction

            else

                echo " PROD optimization"

                APP_ENV=prod php bin/console cache:clear --no-debug
                APP_ENV=prod php bin/console cache:warmup

                APP_ENV=prod php bin/console doctrine:migrations:migrate \
                    --allow-no-migration \
                    --no-interaction

                # Opcional si tens bundle opcache
                APP_ENV=prod php bin/console opcache:clear || true

            fi

            echo " Fixing permissions"

            sudo setfacl -R -m u:www-data:rwX -m u:${{ env.TARGET_USER }}:rwX var/
            sudo setfacl -dR -m u:www-data:rwX -m u:${{ env.TARGET_USER }}:rwX var/

            echo " Reload Apache"
            sudo systemctl reload apache2

            echo " Deploy completat"
